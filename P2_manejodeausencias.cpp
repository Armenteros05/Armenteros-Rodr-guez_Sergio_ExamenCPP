#include <iostream>
#include <string>
using namespace std;
class asistencia {
public:
    asistencia(string fecha, string estado) : fecha(fecha), estado(estado) {}
    string fecha;
    string estado;
